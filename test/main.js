require.config({
    // baseUrl:'/base'
});

debugger;
define("test/main", ['/base/backbone-deferred'], function (backboneDeferred) {
    debugger;
    console.log(arguments);
    return {

    };
});